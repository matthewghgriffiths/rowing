# Dependencies are declared in pyproject.toml (single source of truth).
# This installs the project in editable mode with every optional extra plus
# the dev tooling, which is what the dev container and CI expect.
-e .[all,dev]
